struct IntPair {
    x as int,
    y as int,
}

fn IntPair:sum args p as IntPair return int {
    p "x" ?
    p "y" ?
    +
}

fn IntPair:set_zero args p as IntPair {
    p "x" { 0 } !
    p "y" { p "x" ? } !
}

fn IntPair:print args p as IntPair {
    "x,y = " .
    p "x" ? .
    "," .
    p "y" ? .
    "\n" .
}

fn main {
    IntPair
    "x = " .
    dup "x" ? .
    "\n" .
    // should print "x = 0\n"

    "y = " .
    dup "y" ? .
    "\n" .
    // should print "y = 0\n"

    dup "x" { 3 } !
    dup "y" { 4 } !

    "x = " .
    dup "x" ? .
    "\n" .
    // should print "x = 3\n"

    "y = " .
    dup "y" ? .
    "\n" .
    // should print "y = 4\n"

    dup
    IntPair:sum .
    "\n" .
    // should print "7\n"

    dup
    IntPair:print
    // should print "x,y = 3,4\n"

    dup
    IntPair:set_zero

    dup
    IntPair:print
    // should print "x,y = 0,0\n"

    drop
}
