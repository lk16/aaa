fn main {
    vec[int]
    dup 1 vec:push
    dup 2 vec:push
    dup 3 vec:push
    dup 4 vec:push

    0

    use total {
        foreach {
            total <- { total + }
        }
        total .
        "\n" .
    }
}
