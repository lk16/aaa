struct Bar {
    i as int,
}

struct Foo {
    i as int,
    b as bool,
    s as str,
    m as map[int, int],
    se as set[int],
    v as vec[int],
    ba as Bar,
}

fn main {
    Foo
    use f {
        f "i" ?
        drop

        f "b" ?
        drop

        f "s" ?
        drop

        f "m" ?
        drop

        f "se" ?
        drop

        f "v" ?
        drop
    }
}
